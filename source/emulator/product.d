/**
 * Information about the emulator program.
 */
module emulator.product;

enum name          = "DEMU";  /// Name displayed to the user.
enum shortName     = "demu";  /// Name used as an identifier.
enum versionNumber = 1.0;     /// Release version number.
enum versionString = "1.0.0"; /// ditto
